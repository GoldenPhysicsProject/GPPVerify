# GPPVerify — Claude operating instructions

Operating rules for this repo live in `website/GPPVERIFY.md` (in the
`GoldenPhysicsProject/website` repo), not here — Daniel's explicit standing instruction
is to keep them in one place there rather than split across repos. This file exists only
so a session working directly in this checkout still gets a pointer auto-loaded: go read
`website/GPPVERIFY.md` for the actual rules (non-negotiables, branch hygiene, toolchain,
deploy gotchas, session protocol), and `website/CLAUDE.md` for shared project context
(owner, credentials, Codex division-of-labor).

Do not duplicate content back into this file — it drifted out of sync with
`website/GPPVERIFY.md` once already (2026-08-24) when a session recreated a full copy
here without checking whether one already existed elsewhere. Fix `website/GPPVERIFY.md`
directly instead.
